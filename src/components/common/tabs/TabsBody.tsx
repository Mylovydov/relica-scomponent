import React, { FC } from 'react';

// StyledProps

// Styled

// ComponentProps

export const TabsBody: FC = ({ children }) => {
    return (
        <div>
            {children}
        </div>
    );
};


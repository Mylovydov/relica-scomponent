import React, { FC } from 'react';

import styled from 'styled-components';

// StyledProps
// Styled
// ComponentProps

export const Posts: FC = ({ children }) => {
    return (
        <div>
            {children}
        </div>
    );
};
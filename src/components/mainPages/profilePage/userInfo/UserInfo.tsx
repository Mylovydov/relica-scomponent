import React, { FC } from 'react';

// StyledProps

// Styled

// ComponentProps

export const UserInfo: FC = ({ children }) => {
    return (
        <div>
            {children}
        </div>
    );
};
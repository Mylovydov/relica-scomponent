import React, { FC } from 'react';

import styled from 'styled-components';

// StyledProps

// Styled
// ComponentProps

export const MessageItemSendTime: FC = () => {
    return (
        <div>
            
        </div>
    );
};
import React, { FC } from 'react';

import styled from 'styled-components';

// StyledProps

// Styled

// ComponentProps


export const ChatContactsList: FC = () => {
   return (
      <div>
         
      </div>
   );
};
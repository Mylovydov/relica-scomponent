import React, { FC } from 'react';

import styled from 'styled-components';

// StyledProps

// Styled

// ComponentProps
export const СurrentСommunicationTalk = () => {
    return (
        <div>
            
        </div>
    );
};
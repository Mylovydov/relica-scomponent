import React, { FC } from 'react';

// StyledProps
// Styled
// ComponentProps

export const SendMessage: FC = ({ children }) => {
    return (
        <div>
            {children}
        </div>
    );
};
import React, { FC } from 'react';
import styled from 'styled-components';

// StyledProps
// Styled
const StyledPostCard = styled.div``

// ComponentProps

export const PostCard: FC = (props) => {
   return <StyledPostCard {...props}/>
};
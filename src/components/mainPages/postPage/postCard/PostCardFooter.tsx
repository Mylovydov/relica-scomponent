import React, { FC } from 'react';

import styled from 'styled-components';

// StyledProps
// Styled

const StyledPostCardFooter = styled.div``
// ComponentProps

export const PostCardFooter: FC = (props) => {
   return <StyledPostCardFooter {...props}/>
};
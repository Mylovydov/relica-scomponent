import React, { FC } from 'react';
import styled from 'styled-components';

// StyledProps
// Styled
const StyledPostCardBody = styled.div``
// ComponentProps

export const PostCardBody: FC = (props) => {
   return <StyledPostCardBody {...props}/>
};
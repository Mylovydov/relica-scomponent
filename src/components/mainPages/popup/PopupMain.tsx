import React, { FC } from 'react';

import styled from 'styled-components';

// StyledProps

// Styled

// ComponentProps

export const PopupMain: FC = () => {
   return (
      <div>
         
      </div>
   );
};
import React, { FC } from 'react';
import styled from 'styled-components';

// StyledProps
// Styled
const StyledSubmenuBody = styled.nav``
// ComponentProps


export const SubmenuBody: FC = (props) => {
   return <StyledSubmenuBody {...props}/>
};
import React, { FC } from 'react';

// StyledProps

// Styled

// ComponentProps

export const DetailPostPage: FC = ({ children }) => {
    return (
        <div>
            {children}
        </div>
    );
};
import React, { FC } from 'react';
import styled from 'styled-components';

// StyledProps

// Styled
const StyledLoginWithWalletPageBody = styled.div`

`
// ComponentProps

export const LoginWithWalletPageBody: FC = (props) => {
    return (
        <StyledLoginWithWalletPageBody {...props}/>
    );
};
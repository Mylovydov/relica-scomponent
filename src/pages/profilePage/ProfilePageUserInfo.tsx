import React, { FC } from 'react';

// StyledProps

// Styled

// ComponentProps

export const ProfilePageUserInfo: FC = ({children}) => {
    return (
        <div>
            {children}
        </div>
    )
};